# 傳承（lore）實作規格 — T-33

**這份是實作的唯一規格。** owner 於 2026-09-07 `rc-8ad66d9c91f6` 圈 [0] 核可。
設計背景與取捨在任務產物 `ta-4e133e49a6be`；草圖 https://claude.ai/code/artifact/506201df-5feb-4aff-ba8f-42bc21bf84f1

---

## 0. 紅線（違反要當阻擋項回報，不要自己放行）

- **不准在本機跑整包測試**（`bash bin/ci.sh` 之類）。只跑改動範圍的點名測試：`cd server/ocserverd && go test -run '<Name>' ./...`；前端 `npx vitest run <檔>`。
- **這台機器是 seth-m5，正式站主機**：不准跑端到端、不准起測試站。
- **不自己合併、不自己上站、不自己調 cap 旋鈕。**
- **檔名不要帶 `_token`**（`.gitignore` 會靜默吞掉，`git status` 也看不到）。
- **產生檔只能用產生器**：`ocapi_gen.go` ← `bin/gen-ocapi`；`schema.ts` ← `npm run gen:api`；`mcp-catalog.json` ← `python3 bin/gen-mcp-catalog`（🔴 是 python，用 bash 會語法錯）；`migration.lock` ← `bin/gen-migration-lock`，**不准手改**。
- **`server/ocserverd` 是獨立 go module** ⇒ `cd server/ocserverd && go test ./...`。新工作樹首跑前先 `bash bin/build-seedsdist`、`bash bin/build-docsdist`，否則會有假紅。
- **退出碼一律落檔再讀**（`cmd > out.txt 2>&1; echo $? > rc.txt`）。`cmd; echo "rc=$?"` 回報的是 `echo` 的退出碼。
- **零命中的預設解讀是「查法寫錯了」**，每次配陽性對照。
- `wc -m` 在 `LC_CTYPE=C` 下數位元組 ⇒ 先 `export LC_ALL=en_US.UTF-8`。

---

## 1. 資料模型

**Migration 版號 `00093`。** 🔴 遠端分支已佔到 00092（掃過全部 454 個 `refs/remotes/origin/*`，配過陽性對照）；用「主線最大 +1」＝00089 會撞號。

表 `lore_entry`（單數）：

| 欄位 | 型別 | 說明 |
|---|---|---|
| `id` | TEXT PK | 對外顯示成 `L-<n>`；`n` 全域遞增 |
| `seq` | INTEGER | 全域遞增，供 `L-<n>` 顯示與穩定排序 |
| `scope_kind` | TEXT | `agent` 或 `manual`（🔴 DB 的 CHECK 仍收 `role`，見下方「範圍收斂」） |
| `scope_key` | TEXT | `agent` ⇒ 該成員自己的 member id；`manual` ⇒ task manual 的 `type_key` |
| `title` | TEXT | ≤ 標題上限（預設 80 字） |
| `body` | TEXT | ≤ 內容上限（預設 500 字） |
| `author_id` | TEXT | 寫入者的成員 id，**寫入當下釘死，之後不隨名冊變動** |
| `source_task_id` | TEXT | 可空，寫入時所在的任務，純紀錄 |
| `state` | TEXT | **`active` / `pinned` / `retired` 三選一互斥** |
| `retire_reason` | TEXT | 可空，只在 `retired` 時有意義 |
| `effective_ts` | REAL | 生效期；建立時＝現在 |
| `created_ts` | REAL | **建立時間，永不變動** —— 這是「最初的生效期」，讓「提到最新」可逆 |
| `updated_ts` | REAL | 狀態或理由變動時更新 |

🔴 **條目沒有編輯路徑。** 不提供改 `title` / `body` 的 API。可變的只有 `state`、`retire_reason`、`effective_ts`。
🔴 **`created_ts` 一旦寫入永不變動**，`effective_ts` 才是「提到最新」會蓋掉的那一個。

索引：`(scope_kind, scope_key, state, effective_ts DESC)`。

### 範圍收斂：三種 → 兩種（owner 2026-09-07，卡 `rc-a43100fd0486` 圈 [0]）

逐字裁定：「應該已經沒有角色傳承」「只有成員跟任務傳承兩種」；選項 [0] 是「拿掉角色傳承，現有九筆全部搬到**那個角色底下的那一位成員**底下」。

- Go 這一側 `LoreScopeRole` 已刪除，寫入面與篩選面都不再認 `role`（篩選送 `role` 是 400，不是空頁——空頁會讓舊客戶端以為自己的傳承被刪了）。
- 資料由 `migration_00100_lore_role_scope_to_member.go` 搬遷：**該角色底下恰好一位 `roster_status='active'` 的成員** ⇒ 搬到那一位；**其餘每一種情況（0 位、2 位以上）⇒ 整列不動，留在 `role`**。搬不動的每一組會把 role_key、列數與落到哪一支印到 stderr，goose 的輸出看得到。
- 🔴 **`00093` 的 CHECK 沒有收窄，是刻意的。** 收窄成兩個值需要重建表，而任何一列孤兒都會在重建時失敗——那等於把「分不出這是誰的」變成「這一列不准存在」，正好是搬遷規則要避免的靜默刪除。
- 孤兒在座艙上讀得到：它走前端的 `unknown` 那一支，顯示原始的鍵、**不冠任何種類詞**（冠上去就等於幫它指定了一個搬遷刻意拒絕指定的主人）。
- ⚠️ **這次放棄了什麼**：同一個角色底下若放兩位成員，他們不再共用傳承，各學各的。owner 在圈選前已被逐字告知。而且「一個角色一位成員」**沒有任何地方強制**——`member.role_key` 沒有 UNIQUE 索引，hire 面也不檢查——所以那是今天名冊的樣子，不是不變量。**補上那個約束不在這張票的射程內，是已知缺口。**

---

## 2. 挑條目的邏輯（🔴 只能有一份）

一個函式，三個出口都呼叫它。**不准在任何一條路徑上再寫一份。**

輸入：`scope_kind`、`scope_key`、字數上限。
步驟：
1. 取該 scope 底下 **`state != 'retired'`** 的條目。
2. 排序：**`pinned` 在前，其餘依 `effective_ts` 由新到舊**；同群內也依 `effective_ts` 由新到舊。
3. 依序累加「標題＋內容」的**字數**（Unicode 字元，不是位元組），**裝不下的整筆不載入**（不截斷），並**繼續往下試**還是**就此停住**——🔴 **就此停住**：後面的一定更舊，繼續試會讓載入集合跳號，畫面上那條線也就畫不出來。
4. 回傳被選中的條目，以及**第一個落選的位置**（畫界線要用）。

---

## 3. 三個出口

**成員傳承 · 正職**（`scope_kind='agent'`，`scope_key` ＝**該成員自己的 member id**）
接在正職開機檔的「長期筆記」區塊後面。
⚠️ **這個出口是有條件的，其他兩個不是。** `buildBootContext` 同時也是座艙的**角色預覽**（不帶 member 呼叫），那條路徑上沒有 member id 可以當鍵，於是**整塊不出**——不是出一塊空的，也不是退回用 role_key。用 role_key 等於把這次拿掉的範圍再變出來；挑「那個角色底下的成員」則會讓預覽把某一個人的傳承當成角色的東西顯示。

**成員傳承 · 外包**（同一個 `scope_kind='agent'`，`scope_key` ＝該外包自己的 member id）
接在外包開機檔裡。owner 核可於卡 `rc-3c24fdc61ed3`：外包學到的東西原本沒有地方放，於是它每醒來一次就從頭開始一次。
🔴 **這兩個出口現在問的是同一個 scope、花的是同一個旋鈕**，只是分別由兩份各自寫的組裝程式碼呼叫。所以「兩個出口會不會分岔」現在問的是**它們各自指名了哪一位成員**，不再是「哪一種 kind」。
字數旋鈕仍叫 `lore.cap_chars.role`——**名字過時了，意思是「每一個成員範圍的預算」**。沒有改名，因為改一個 owner 已經轉過的設定鍵是他的決定，不在這張票裡。
⚠️ **這是外包開機檔裡唯一因人而異的內容。** 那份文件的其餘部分對每個外包都一樣。

**任務傳承**（`scope_kind='manual'`，`scope_key` ＝手冊類型）
接在 `GET /api/task-manuals/{type_key}` 回應裡 learnings 的後面。**不進任何人的開機檔**，正職與外包一視同仁。

🔴 **三個出口共用同一份挑選邏輯**（`selectLoreForScope`），各自傳不同的 scope 進去。在任何一條路徑上另寫一份挑選，症狀會是「同一筆條目在一個出口看得到、在另一個出口看不到」，而且不報錯。
🔴 除了接上這三個出口所需的改動之外，**不要去抽正職與外包共用開頭那段既有的重複** —— 那段跟傳承無關，屬於 T-14 的範圍，不在本次授權內。

上限：所有成員範圍共用一個（旋鈕鍵仍叫 `lore.cap_chars.role`），任務手冊自己一個，**不相加**。

---

## 4. 設定：新增四列

加在既有的參數調整清單上（`SettingsPage.tsx` 的 `DOC_CAP_FIELDS` / `DOC_CAP_ORDER` 是表驅動的，加旋鈕＝加表項＋一對 i18n key，**不是新頁面**）：

| 列 | 預設 |
|---|---|
| 成員傳承字數上限 | 10000 |
| 任務傳承字數上限 | 10000 |
| 傳承標題字數上限 | 80 |
| 傳承內容字數上限 | 500 |

這四列跟其餘既有旋鈕一樣**兩個方向都能轉**（owner 2026-09-07 於卡 `rc-5b66ba099e28` 圈 [1]：八個旋鈕共用一個地板，「只能調高」那條規則整站取消）。對傳承而言調低只影響下次載入哪幾筆，卡不住任何已經寫進去的條目。owner 當天自己就把標題從 140 調到 80、內容從 1000 調到 500。

---

## 5. MCP 工具

寫入只走 MCP，座艙不做撰寫表單。

- **寫一筆**：`title`、`body`、可空的 `task_id`。
  - `task_id` 空 ⇒ 寫進**呼叫者自己的開機檔**：`scope_kind='agent'`、`scope_key` ＝**呼叫者自己的 member id**，正職與外包一視同仁（2026-09-07 範圍收斂之後這裡不再分兩支）。
    名冊上沒有這個人（owner 沒有名冊列）⇒ 400，訊息要指路去寫任務傳承。權限層級在成員門檻以下的身分（機器守衛）連這支介面都呼叫不到，是 403。
  - `task_id` 有 ⇒ 取該任務的 `type_key`。**有類型 ⇒ 寫進那本手冊。**
  - 🔴 **`type_key` 是空的（臨時任務）⇒ 跟沒填任務一樣，寫進呼叫者自己的開機檔**（owner 於 `rc-35db15f76c66` 更正：「臨時任務跟無關乎任何任務一樣都是給 NULL」）。
    這一格曾經是 400，理由是「默默歸到寫入者自己的開機檔會讓每個人開機都付字數、而需要它的類型永遠拿不到，且不報錯」。**那個危害是靠不報錯成立的**，所以現在的做法不是把它放行，是把「默默」拿掉：回應會帶一句 `scope_note`，逐字說出這一筆寫進了哪裡、以及為什麼不是任何一本手冊。**沒有那一句，這一格就要退回 400。**
  - 超過標題或內容上限 ⇒ 400，**一個字都不寫**。
- **改狀態**：`entry_id` ＋ 目標狀態（`active` / `pinned` / `retired`），可帶 `retire_reason`。
- **提到最新**：`entry_id` ⇒ `effective_ts` ← 現在。`created_ts` 不動。
- **讀清單**：依 scope 篩選、分頁。

---

## 6. 前端清單頁

自己的分頁。**整套照任務卡的設計語言**（去讀 `TaskCard.tsx` / `tasks.css` 實際的樣子，不要照轉述）：

- 整列可點展開／收合，內容預設一行截斷
- 右上角一顆 chevron 指示器（inline SVG，無邊框無背景，展開是換一顆圖不是旋轉）
- **一顆狀態徽章**，點了在徽章左緣下方開小選單選 `失效 / 生效 / 置頂`，當前值用強調色＋700
- 「撰寫人」自成一列：標籤在左、頭像＋名字膠囊在右，**傳訊息圖示在膠囊裡**（點了跳聊天頁）；**撰寫人已不在名冊上就不顯示那顆圖示**
- **不做轉派**
- 生效期與「提到最新」同一行，生效期靠左、按鈕靠右
- **失效理由沒填就整列不顯示**
- 三群：置頂 → 生效中 → 已失效。**排序固定不給改**，篩選可以
- **界線**：篩選收斂到單一主詞時才畫一條具名上限線，線以下的條目**內容**轉灰（狀態徽章與按鈕維持正常色）。沒篩選就不畫線也不轉灰。**不要**統計數字、進度條、比例數字
- **條目列上要標「屬於」，而且標在收合的列上**（owner 卡 `rc-11734523eb52` 推翻了原本「不標」的規定：頁面預設打開是「全部」，各種範圍混在一起，讀的人分不出哪一筆是誰的、哪一筆是某本手冊的。標的格式是「是哪一種 · 是哪一個」——只標名字的話，一位叫「特助」的成員跟一本叫「特助」的手冊在列上長得一樣）
- **篩選列照抄任務頁，三格**（owner 2026-09-07，卡 `rc-a43100fd0486`：「我從使用者或是任務手冊作為 filter 另外就是狀態 三個而已」「你完全可以抄 task」）：**撰寫人 ｜ 屬於 ｜ 狀態 ｜ 清除篩選**。任務頁還有一個「任務編號」搜尋框，這頁沒有——傳承的編號不是誰會去打的東西。
  - 「屬於」是一個複選，內容是**每一位成員與每一本任務手冊**，選項的字冠上「成員傳承／任務傳承」，跟列上的「屬於」同一份 i18n 來源。
  - 原本的「範圍」下拉**整個拿掉**：它的三個選項就是這次要收斂掉的東西，剩下兩個之後正好就是「屬於」的兩半，兩格會變成同一個問題問兩次。原本「角色」「手冊」兩個鍵清單也一併拿掉。
  - 🔴 **不加統計數字**（owner：`we dont need count`）。指的是任務頁「負責人」那種**每個選項後面的計數徽章**；傳承是往下捲載入的，這裡算得出來的只有「目前已載入幾筆」，會隨著捲動一直變。膠囊本身的「· N」是「勾了幾個」，那是共用元件的行為，不在拿掉之列。
- 捲到底載 30 筆，**分頁要在伺服器端帶篩選一起走**，否則計數與界線會錯、且「捲到底沒有了」跟「真的沒有了」長得一樣

---

## 7. 測試的判準

🔴 **綠燈沒有證據力**：判準是「把修補拿掉，對應的測試真的會紅」。
- 種 mutant 至少 4 顆，一顆只改一處，每顆前 `go clean -testcache`，還原用自己備份的檔案（**不要 `git checkout --`**，那是整檔粒度，會靜默清掉同檔其他未提交改動）。
- 🔴 特別驗「挑條目真的挑對了」：排序、跳過失效、裝到上限就停。**這是最容易做出假綠的一格。**
- 恆真斷言檢查：新斷言套回改動前的碼上跑，必須真的失敗。
