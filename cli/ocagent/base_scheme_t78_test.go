// Skeleton generated from cli/ocagent/base_scheme_t78.go by gen_test_skeletons.py.
// Every case is a t.Skip placeholder: fill the body, keep or rewrite the name.

package main

import "testing"

func TestIsLoopbackHost(t *testing.T) {
	t.Skip("TODO: ── T-78 CANONICAL BLOCK — DO NOT EDIT ONE COPY ───────────────────────────── This block is duplicated VERBATIM in three modules and bin/tests/base-scheme- mirror-guard.sh fails the build if the copies ever diverge: server/ocserverd/base_scheme_t78.go — what the server HANDS OUT cli/ocwarden/base_scheme_t78.go — what the warden CALLS and what it writes into each agent's .mcp.json cli/ocagent/base_scheme_t78.go — what the agent CALLS It is copied rather than shared because these are four separate Go modules with no common one; a shared package is the right fix and is deliberately NOT being done under an incident clock.")
}

func TestSchemeForHost(t *testing.T) {
	t.Skip("TODO: schemeForHost is the whole of the rule.")
}

func TestNormalizeBase(t *testing.T) {
	t.Skip("TODO: normalizeBase keeps the HOST of a stored base URL and re-decides its scheme.")
}
