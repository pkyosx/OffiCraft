// Skeleton generated from server/ocserverd/routes.go by gen_test_skeletons.py.
// Every case is a t.Skip placeholder: fill the body, keep or rewrite the name.

package main

import "testing"

func TestVerifyDiffShareSig(t *testing.T) {
	t.Skip("TODO: verifyDiffShareSig is GET /api/diff's subject: both addresses and both labels, read RAW (never trimmed — a padded address is a different address).")
}

func TestRouteSpecs(t *testing.T) {
	t.Skip("TODO: routeSpecs builds the route table over the generated wrapper (which binds path/query params, then dispatches into apiServer).")
}
