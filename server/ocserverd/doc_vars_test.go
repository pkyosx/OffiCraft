// Skeleton generated from server/ocserverd/doc_vars.go by gen_test_skeletons.py.
// Every case is a t.Skip placeholder: fill the body, keep or rewrite the name.

package main

import "testing"

func TestDocVarsIn(t *testing.T) {
	t.Skip("TODO: DocVarsIn lists the variable names text uses, in first-appearance order and deduped.")
}

func TestDocVarsUndeclared(t *testing.T) {
	t.Skip("TODO: DocVarsUndeclared lists the names text uses that declared does not allow.")
}

func TestRenderDocVars(t *testing.T) {
	t.Skip("TODO: RenderDocVars substitutes values into text's {name} slots.")
}

func TestDocVarNameList(t *testing.T) {
	t.Skip("TODO: docVarNameList renders names the way both the refusal and the error quote them, so a reader comparing a rejection with a document sees the same shape.")
}

func TestDocVarWriteRefusal(t *testing.T) {
	t.Skip("TODO: docVarWriteRefusal is the ONE refusal text behind the write-face validation, the same way docCapRefusal is the one text behind the cap.")
}
