# T-125 — 這一包刪掉的「文件型測試」，以及每一個該怎麼換掉

owner 於 `c-c2d4d068a694` 指示：「如果有跟文件相關的，一樣刪掉，但列出來我們討論要怎麼處理」。

這份清單就是那個「列出來」。**所有列在這裡的檔案都已經在這一包裡被刪除**，本文件記錄它們原本在做什麼、屬於哪一類、以及建議用什麼取代。

量在 `3bfc3b0f469a5cbd0d8d05f408829fd92f57eb74`。

---

## 這一類東西是什麼

「文件型測試」不是在測某支函式，而是在斷言下面兩種事情之一：

1. **兩份東西必須對得上**（例如：規格文件裡的表格 ⇄ 程式碼裡的常數）
2. **全樹掃描必須沒有命中**（例如：某個已退休的說法不得出現在任何檔案裡）

判定方式：全 401 個 Go 測試檔逐檔比對「它測的是哪個產品檔的哪個函式」，**12 個檔完全對不上任何產品函式**。這 12 個就是這一類的核心。

---

## 甲、可以改成產生器 ⇒ 刪掉測試，改寫產生器

產生器讓不一致**不可能發生**；測試只是事後發現不一致。兩者不是同一個等級的保證。

| 原檔案 | 它在斷言什麼 | 建議怎麼換掉 |
|---|---|---|
| `wording_cap_mirror_test.go` | Go 的某個上限常數必須等於 TypeScript 那份 | 一個來源檔產生兩份。這個 repo 已經有 `REGEN_PAIR_GATE` 在對三組東西做同一件事，照抄即可 |
| `auth_refusal_wire_t14_test.go` | 用程式碼解析器讀 `cli/ocagent` 的原始碼，比對兩邊的連線協定常數 | 同上，常數集中成一份來源再產生 |
| `cli/ocwarden/canon_port_test.go` | 用正則從伺服器的設定檔撈預設 port，比對本模組的字面值 | 同上 |
| `sse_topics_spec_test.go` | 解析 `spec/sse.md` §3.1 的表格，跟 Go 端的主題集合比對 | **測試裡已經寫好 parser 了**，把 parser 搬到產生器，直接產出 Go 與 TypeScript 兩份。⚠️ 有三個 directed band 故意不在表裡，產生器要照抄這個排除規則 |
| `cli/ocagent/uplink_manifest_test.go` | 0 支測試，純粹是給同 package 其他測試用的輔助函式（讀 `cli/uplinks.json`） | 隨著它服務的那些測試一起消失；如果新測試還需要，改寫成一般的輔助函式 |

**另外三組不在這 12 個檔裡、但屬於同一類**（它們對得上產品函式，所以沒被判成 none）：

| 主題 | 現況 | 建議 |
|---|---|---|
| 錯誤碼表 | 一份純資料 JSON，Go 與 TypeScript 各自手寫一份對照 | ★ **建議第一個做**。兩邊都是直譯，沒有人的判斷夾在中間，零 wire 風險 |
| namespace 推導 | 同一條字串推導手抄在 **6 個檔、11 個站點** | ★ **建議第二個做**。守衛腳本自己的註解寫著這份手抄清單**已經錯過四次**，而且每一次讀起來都像是完整的 |
| 路由面 | 183 條路由手寫了四份（規格檔、路由表、conformance 清單、認證矩陣） | 收益最大（可砍 19 支），但需要先把認證與權限兩欄搬進規格檔——那是 wire spec 變更，要 owner 過目 |

---

## 乙、不能改成產生器 ⇒ 需要以測試以外的形式重建

這些的兩邊都是**人的獨立決定**，不是機械推導關係。產生器在這裡不適用。

| 原檔案 | 它在斷言什麼 | 為什麼不能產生 |
|---|---|---|
| `api_signing_key_observation_reach_t80_test.go` | 用程式碼解析器斷言某個函式在產品碼裡**只有恰好一個呼叫點** | 這是對程式碼結構的約束，不是兩份資料的一致性。⚠️ 檔頭自承限制：透過 method value 呼叫會漏掉 |
| `db_singlefile_copy_guard_test.go` | 全樹掃描必須零命中：禁止對資料庫檔案用檔案系統層級的複製（在 WAL 模式下會產生看起來成功、實際損毀的備份） | 這是禁止一種寫法，不是比對兩份東西 |
| `taskno_prose_guard_test.go` | 全樹散文掃描必須零命中：某個已退休的說法不得出現在工作樹任何地方 | 同上 |
| `migration_00086_blank_link_url_test.go` | 種一筆特定資料、跑某個資料庫遷移、斷言不出錯 | 這其實是一支**正常的測試**，只是它測的目標是 SQL 檔不是 Go 檔，所以對照表對不上 |

⚠️ **`db_singlefile_copy_guard_test.go` 與 `taskno_prose_guard_test.go` 這兩支正是 owner 指名要清掉的形狀**——它們列舉「不准這樣寫」，維護成本隨壞法數量成長。owner 逐字：「**我們不可能維護一百種被弄壞的方式**」。刪掉是對的。
**代價要講清楚**：刪掉之後，「有人用檔案系統複製資料庫」與「有人把退休的說法寫回文件」這兩件事不再有任何東西會發現。如果要保留保護，正確的形式是**寫在文件與程式碼審查清單裡**，或者做成 lint 規則，而不是測試。

---

## 丙、已經是產生的，測試在重複做產生器已經保證的事 ⇒ 直接刪，不用補

| 原檔案 | 為什麼是重複的 |
|---|---|
| `frontend/src/api/seeds.test.ts` 的比對部分 | 建置工具讓兩邊在編譯期就是**同一個檔案**，沒有第二份副本。測試把同一個檔案讀兩次再比對相等。**測試檔自己承認了**：「Drift itself is now structurally impossible — there is no second copy」 |

（這一組只有這一項。原本預期會很大，實測結果不是——這個 repo 的一致性檢查設計得相當克制，大部分看起來像重複的測試，實際上都在守產生器沒有涵蓋的那一面。）

---

## 🔴 一個先前弄錯、在此更正的結論

先前曾判定「工具目錄與規格檔的比對測試是重複的、該砍」。**這是錯的，撤回。**

`bin/gen-mcp-catalog` **不是推導器，是串接器**：它把規格檔裡每個操作那段**手寫的**參數宣告**逐字原樣**貼進工具目錄，只驗名稱與說明兩個欄位。它從來沒有從操作的實際參數推導出工具目錄的參數宣告。

⇒ 工具目錄雖然是產生的，**參數集合仍然是手寫的**，`drift-mcp-catalog` 對參數不一致完全看不見。那支比對測試是唯一會發現不一致的東西，**屬甲組不屬丙組**。

**而且這個不一致現在就存在**：`spec_catalog_conformance_test.go` 有一張已知漂移白名單，記錄了 **6 個**規格檔收下、處理程式真的會讀、但工具目錄沒有宣告的參數。註解原文：「an agent, whose only view of a tool is tools/list, cannot discover a feature that works」。

**判準**：「這份檔案是產生的」不等於「它的每一個欄位都是推導出來的」。要去看產生器**實際推導了哪幾欄**。

---

## 這一包裡兩個「名字叫測試、其實不是測試」的東西

有兩段程式借用測試機制，只是為了取得「在 package 內部執行程式碼」這個入場券（Go 的一個資料夾就是一個 package，這是唯一的門）：

| 它實際是什麼 | 為什麼非得在 package 內部 |
|---|---|
| `migration.lock` 的**產生器** | 列舉遷移要讀 `go:embed` 的 FS、還要 AST 掃這個 package；拆成 `bin/` 底下的獨立程式會逼出**第二份列舉實作**，而兩份實作悄悄分歧正是這件事最可能出錯的方式——檢查會拿一份沒人寫過的清單去驗，而且是綠的 |
| 佈景主題名稱比對的 **Go 半邊** | 它要呼叫 `validateThemeBundles`。它本身不做任何斷言，只是把 Go 這一邊對 61 個名稱的判定寫出來，由前端那支測試比對。存在的理由是 Go 與 JavaScript 各自帶自己的 Unicode 表，版本升級時間不同就會分歧 |

### 🔴 中途走過一次錯路，在此更正

第一版的做法是：檔名 `*_tool_test.go` ＋ 編譯標記 `//go:build octool`，讓平常的 `go test ./...` 看不到它們。**owner 否決了**（逐字）：

> 「但這種應該屬於CI check，不要叫做什麼__test」
> 「然後放在bin下面讓makefile包裝再讓workflow呼叫」

⇒ **判準：「在 package 內部」是 Go 的限制，不是「要寫成測試」的理由。子指令同樣在 package 內部，而且它的名字說的就是它做的事。** 標記那條路只是把一個錯誤的名字用另一個機制蓋起來，讀的人還是先看到 `_test.go`。

現在的形狀（`//go:build octool` 已完全不存在）：

| 東西 | 現在住哪 | 誰呼叫 |
|---|---|---|
| 共用列舉、render/parse、兩個判定 | `server/ocserverd/migration_lock.go`（一般原始檔） | 子指令與測試都呼叫同一份 |
| 產生器 | `ocserverd migration-lock --write` | `bin/gen-migration-lock` |
| 清單對不對得上這棵樹 | `ocserverd migration-lock --check` | `bin/check-migration-lock` → `make drift-migration-lock` → workflow |
| 佈景主題名稱的 Go 判定 | `ocserverd theme-name-verdicts <cases> <out>` | `frontend/src/lib/themeName.parity.test.ts` |
| 不准動已上線的遷移 | `bin/check-released-migrations`（讀 git，不讀 lock） | `make check-released-migrations` → workflow |

### ⚠️ 遷移清單那一組：真正的測試留下來了

`migration.lock` 是一份清單，記錄到目前為止有哪幾支資料庫遷移、每一支的內容是什麼。它存在的目的是**防止有人偷改已經上線過的遷移腳本**——那會造成新舊環境的資料結構不同，而且不會有任何症狀。

`migration_lock_test.go` 與 `migration_lock_judgement_test.go` **維持一般測試**，照常在 CI 裡跑；它們測的就是搬到 `migration_lock.go` 的那些函式。

🔴 **第一版我把整組四個檔都當成零件標記起來，那是錯的。** 整組標記之後，那兩支真正的檢查會**從 CI 消失**，而「檢查沒跑」跟「檢查通過」在 CI 畫面上長得一模一樣。
⇒ **判準：分類的軸是「它是不是測試」，不是「它屬不屬於這個主題」。同一個主題裡可以同時有零件與真測試。**

🔴 **兩道檢查不能互相代替，而且很容易以為可以。** 編輯一支已上線的遷移之後跑產生器，`--check` 會變綠——因為 lock 現在誠實地描述了這棵樹，那是它唯一宣稱過的事。說「這個編輯不被允許」需要基準線，那是 `bin/check-released-migrations` 的工作，所以它讀 `git diff`、不讀 lock：它必須在 lock 被重新產生之後還活著。
